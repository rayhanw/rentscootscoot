<div class="container index-list">
  <div style="margin-top: 5rem;">
    <h1 class="text-center">My bookings</h1>
  </div>

  <div class="m-5">
    <% @bookings.each do |b| %>
      <div class="card-product mb-3">
        <%= cl_image_tag b.scooter.photo, width: 120, height: 120  %>
        <div class="card-product-infos">
          <h2 class="text-left"><%= b.scooter.name %></h2>
          <p><%= b.start_date %> - <%= b.end_date %></p>
          <p><%= b.scooter.description %></p>
          <p id="price"></p>
        </div>
        <div class="card-product-actions">
          <h6 class="text-right">
            <%= link_to edit_booking_path(b) do %>
              <i class="fas fa-edit" style="color: #F9A468"></i>
            <% end %>
          </h6>

          <h6 class="text-right">
            <%= link_to "Cancel booking", booking_path(b), method: :delete, class: 'btn btn-warning', data: { confirm: 'Are you sure?' } %>
          </h6>

          <h6 class="text-right">
            <%= link_to "Add review", booking_path(b), class: 'btn log-in review-add' %>
          </h6>
        </div>
      </div>
    <% end %>

    <%= link_to "Back to list", scooters_path, class: 'btn sign-up mt-5'  %>
  </div>
</div>
